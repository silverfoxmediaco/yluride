{
  "name": "yluride",
  "version": "1.0.0",
  "description": "YLURide - Luxury Mercedes Van Rental Service",
  "scripts": {
    "build": "cd backend && npm install && cd ../frontend && npm install --legacy-peer-deps && npm run build && cp -r build ../backend/",
    "start": "cd backend && node server.js"
  },
  "keywords": ["luxury", "van", "rental", "mercedes", "transportation"],
  "author": "Silver Fox Media Co",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}